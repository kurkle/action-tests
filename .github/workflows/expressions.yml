name: Expressions

on: [push, release]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test different expressions
        run: echo "${concat}"
        env:
          trim: ${{ github.event_name[1] }}
          concat: chart.js-${{ github.event_name }}.tgz

