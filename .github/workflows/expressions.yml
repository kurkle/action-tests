name: Expressions

on: [push, release]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Text different expressions
        run: echo "${concat}"
        env:
          trim: ${{ github.event_name }}
          concat: chart.js-${{ github.event_name }}.tgz

